# Dependencies and build output
node_modules/
build/
dist/
coverage/
.cache/

# Pre-deletion staging for the 0.1.0 clean restart. Everything in here is
# already in git history (see the commits before the restart), so this copy is
# a convenience only. Delete the whole directory before the 0.1.0 release.
archived/

# Local-only hardware inputs and captures
local/
.local/
captures/
fixtures/captures/
isdbt_rio.inp
*.bin
*.fw
*.rom
*.dump
*.pcap
*.pcapng
*.m2ts
*.mts
*.mp4

# UI のモック用動画。実 live 経路へ繋ぐまでの仮置きで、成果物には載せない。
public/mock-stream.mp4

# Local configuration and credentials
.env
.env.*
!.env.example

# Editors and operating systems
.DS_Store
Thumbs.db
*.swp
*.swo
